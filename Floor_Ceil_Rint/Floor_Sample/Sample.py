import numpy as np


def floors(lists):
    array = np.array(lists)
    return np.floor(array)
def ceils(lists):
    array = np.array(lists)
    return np.ceil(array)
def rints(lists):
    array = np.array(lists)
    return np.rint(array)